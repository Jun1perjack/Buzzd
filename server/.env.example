PORT=3000
NGROK_AUTHTOKEN=your_ngrok_authtoken_here
VERCEL_URL=https://buzzd.vercel.app

# Optional: set a fixed room code instead of random each session
# ROOM_CODE=ABCD12

# Virtual controller uses Python (python3 + evdev must be installed):
#   pip3 install evdev
# If evdev is unavailable the server still runs in fallback (log-only) mode.
